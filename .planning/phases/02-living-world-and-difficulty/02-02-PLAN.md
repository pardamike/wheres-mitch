---
phase: 02-living-world-and-difficulty
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/core/difficulty.ts
  - src/game/input.ts
  - src/world/difficulty-profile.ts
  - src/world/mitch.ts
  - src/world/path-network.ts
  - src/world/scene.ts
  - src/scenes/washington.ts
  - src/render/stage-renderer.ts
  - tests/unit/difficulty.test.ts
  - tests/unit/mitch.test.ts
  - tests/e2e/occlusion.spec.ts
autonomous: true
requirements:
  - MITCH-02
  - MITCH-03
  - MITCH-04
  - MITCH-05
  - MITCH-06
---

<objective>
Give Turtle Mitch reproducible route-and-hide intelligence, genuine visual/input occlusion, fair
early reveals, and a monotonic difficulty curve that eventually outruns human tracking.
</objective>

<execution_context>
Read the exact formulas and fairness rules in `docs/GAME-DESIGN.md`, SVG/input contracts in
`docs/TECHNICAL-SPEC.md`, `02-CONTEXT.md`, `02-RESEARCH.md`, `02-UI-SPEC.md`, and the completed
route/crowd code.
</execution_context>

<tasks>

<task type="execute">
  <name>Encode and property-test the canonical difficulty profile</name>
  <read_first>
    `docs/GAME-DESIGN.md` Difficulty Progression; `src/core/difficulty.ts`,
    `src/world/difficulty-profile.ts`, and `tests/unit/difficulty.test.ts` if present.
  </read_first>
  <files>
    `src/core/difficulty.ts`, `src/world/difficulty-profile.ts`,
    `tests/unit/difficulty.test.ts`
  </files>
  <action>
    Implement the eight canonical formulas exactly, using completed rounds as the zero-based input:
    crowd count capped at 96, crowd speed capped at 1.65, Mitch speed capped at 2000, decision/
    dwell/peek/hidden timings floored as specified, and hitbox scale floored at 0.72. Validate input
    and return finite values. Test the round-one baseline, caps/floors, monotonic direction across
    rounds 0–100, and documented late-game impractical thresholds. Keep one function as the source
    of truth for UI, world, and tests.
  </action>
  <acceptance_criteria>
    Every completed round increases at least one effective challenge dimension and no dimension
    reverses toward easier; extreme rounds remain finite and intentionally very fast.
  </acceptance_criteria>
  <verify>`npm run test:run -- difficulty`; compare constants to `docs/GAME-DESIGN.md`.</verify>
</task>

<task type="execute">
  <name>Implement continuous seeded Mitch routes, hiding, and fairness</name>
  <read_first>
    `src/world/mitch.ts`, `src/world/path-network.ts`, `src/world/scene.ts`,
    `src/scenes/washington.ts`, `src/core/rng.ts`, and `tests/unit/mitch.test.ts` if present.
  </read_first>
  <files>
    `src/world/mitch.ts`, `src/world/path-network.ts`, `src/world/scene.ts`,
    `src/scenes/washington.ts`, `tests/unit/mitch.test.ts`
  </files>
  <action>
    Model Mitch as route transit, approach, peek, dwell/hidden, and depart states driven by the
    round's named Mitch RNG. Select reachable hiding spots, avoid an immediate duplicate when an
    alternative exists, and traverse all intervening route edges. Apply profile timings/speed and
    hitbox scale. In early profiles, constrain hidden duration and schedule exposed transit/peek
    windows; allow the canonical floors to make later play absurd. Write trace-oriented unit tests
    proving continuity, reachability, timing bounds, different/same seed behavior, and pause-neutral
    updates. Never teleport on ordinary decisions.
  </action>
  <acceptance_criteria>
    Mitch visibly travels between authored spots; seed plus inputs reproduce route and timing; early
    rounds expose him within bounds; late rounds use the exact profile.
  </acceptance_criteria>
  <verify>`npm run test:run -- mitch`; inspect deterministic route trace for seed 324001.</verify>
</task>

<task type="execute">
  <name>Make authored foreground coverage block target activation</name>
  <read_first>
    `docs/TECHNICAL-SPEC.md` SVG Depth/Occlusion and Input; `src/render/stage-renderer.ts`,
    `src/scenes/washington.ts`, `src/game/input.ts`, `tests/e2e/occlusion.spec.ts` if present.
  </read_first>
  <files>
    `src/render/stage-renderer.ts`, `src/scenes/washington.ts`, `src/game/input.ts`,
    `tests/e2e/occlusion.spec.ts`
  </files>
  <action>
    Place painted authored occluders above Mitch and keep their pointer events active. Disable
    pointer events only on transparent decoration. Couple Mitch visual state and hit target so a
    fully hidden portion cannot remain clickable, while an exposed peek has a scaled target matching
    its visible silhouette. Add deterministic browser coverage that clicks the same apparent Mitch
    location when covered (miss) and exposed (catch), using debug state to schedule state rather
    than production-only bypass behavior.
  </action>
  <acceptance_criteria>
    Opaque foreground geometry produces a miss and consumes one click; exposed target catches; open
    transparent regions do not create invisible click blockers.
  </acceptance_criteria>
  <verify>`npm run test:e2e -- occlusion`; manually inspect every Washington hiding spot.</verify>
</task>

</tasks>

<verification>
Run unit, Phase 1 E2E, and occlusion tests. Play rounds 1, 6, 12, and 25 with fixed seeds and compare
profile/debug traces. Review all hiding spots visually at desktop and mobile landscape.
</verification>

<success_criteria>
The target behaves like a mischievous continuous character, is click-blocked by what the player
sees, starts fair, scales monotonically, and becomes hilariously unmanageable.
</success_criteria>

<must_haves>
  <truths>
    MITCH-02 through MITCH-06 and decisions D2-04 through D2-08 are proven by rules, rendering, and
    browser behavior.
  </truths>
  <artifacts>
    Difficulty and Mitch modules, authored Washington hiding data, and focused unit/E2E tests exist.
  </artifacts>
  <key_links>
    Reducer round state selects one difficulty profile; the profile configures crowd and Mitch;
    Mitch state drives both visual exposure and target hit behavior; occluders stay above it.
  </key_links>
</must_haves>

<threat_model>
Bound round indices and formulas to finite safe outputs. Debug forcing accepts allowlisted scene/
round/seed values only and must not expose code execution or markup injection. Pointer geometry,
not hidden bypass flags, determines ordinary catches.
</threat_model>
