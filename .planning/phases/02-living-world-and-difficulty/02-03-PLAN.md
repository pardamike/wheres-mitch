---
phase: 02-living-world-and-difficulty
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - package.json
  - src/core/clock.ts
  - src/game/controller.ts
  - src/game/visibility.ts
  - src/world/actor.ts
  - src/world/mitch.ts
  - src/render/stage-renderer.ts
  - src/render/ui-renderer.ts
  - tests/unit/clock.test.ts
  - tests/unit/reducer.test.ts
  - tests/e2e/pause.spec.ts
  - tests/e2e/performance.spec.ts
  - docs/PERFORMANCE.md
autonomous: false
requirements:
  - LOOP-08
  - QUAL-03
---

<objective>
Make simulation time correctly pause for the player and browser, remove steady-state allocation
hotspots, and record evidence that the 96-actor SVG world meets desktop/mobile performance targets.
</objective>

<execution_context>
Read `02-CONTEXT.md` Performance Budget, `02-RESEARCH.md`, `02-UI-SPEC.md`,
`docs/TECHNICAL-SPEC.md` Animation section, `docs/TEST-PLAN.md` performance/pause criteria, and all
current controller/world/render code.
</execution_context>

<tasks>

<task type="execute">
  <name>Centralize clamped simulation time and pause reasons</name>
  <read_first>
    `src/core/clock.ts`, `src/game/controller.ts`, `src/game/visibility.ts`,
    `src/core/reducer.ts`, `src/render/ui-renderer.ts`, and related tests if present.
  </read_first>
  <files>
    `src/core/clock.ts`, `src/game/controller.ts`, `src/game/visibility.ts`,
    `src/render/ui-renderer.ts`, `tests/unit/clock.test.ts`, `tests/unit/reducer.test.ts`,
    `tests/e2e/pause.spec.ts`
  </files>
  <action>
    Implement one monotonic simulation clock driven by animation-frame timestamps and clamp a frame
    delta to 50 ms. Track manual and document-hidden pause reasons independently so clearing one
    cannot resume while the other remains. On visibility loss, suspend immediately; on return,
    remain manually paused when applicable, otherwise show the canonical READY — SET — FIND!
    countdown before resuming. Do not advance world, Mitch, transient effects, outcome timelines,
    or visible-round duration while suspended. Prevent a catch-up frame. Add fake-clock and browser tests for nested reasons,
    unchanged click/state/time, and exact-pose resume.
  </action>
  <acceptance_criteria>
    Manual pause and tab hiding freeze all simulation; returning never consumes attempts, skips an
    outcome, or advances Mitch; only valid resume conditions restart the clock.
  </acceptance_criteria>
  <verify>`npm run test:run -- clock reducer`; `npm run test:e2e -- pause`.</verify>
</task>

<task type="execute">
  <name>Remove steady-state DOM and allocation bottlenecks</name>
  <read_first>
    `src/game/controller.ts`, `src/world/actor.ts`, `src/world/mitch.ts`,
    `src/render/stage-renderer.ts`, and `docs/PERFORMANCE.md` if present.
  </read_first>
  <files>
    `src/game/controller.ts`, `src/world/actor.ts`, `src/world/mitch.ts`,
    `src/render/stage-renderer.ts`, `docs/PERFORMANCE.md`
  </files>
  <action>
    Profile the production build at the maximum 96-actor debug scenario before changing code.
    Record the largest costs. Pool actor/effect nodes, reuse snapshot buffers where clear, avoid
    per-frame selector queries and layout reads, batch transform/visibility writes, and simplify
    expensive SVG decoration only where the profile identifies it. Preserve direct readable code;
    do not create a generic performance framework or switch renderer without an explicit owner
    decision. Document reference hardware/browser/viewport and before/after measurements.
  </action>
  <acceptance_criteria>
    The frame loop creates no DOM nodes and no arrays proportional to actor count; measurements and
    any scoped optimizations are recorded with reproducible setup.
  </acceptance_criteria>
  <verify>
    Browser performance recording, stable DOM-node-count observation over five minutes, and source
    review for reads-after-writes in the frame loop.
  </verify>
</task>

<task type="execute">
  <name>Automate the repeatable performance scenario and enforce practical floors</name>
  <read_first>
    `docs/TEST-PLAN.md` Performance Checks; `tests/e2e/performance.spec.ts`,
    `playwright.config.ts`, `docs/PERFORMANCE.md` if present.
  </read_first>
  <files>`tests/e2e/performance.spec.ts`, `docs/PERFORMANCE.md`, `package.json`</files>
  <action>
    Add a fixed-seed 96-actor production-build scenario that samples animation-frame intervals for
    30 stable seconds after warmup and reports p50/p95, long frames, browser, viewport, and reduced
    motion. Keep CI assertions tolerant enough for shared hardware while failing obvious regressions
    such as sustained sub-30 FPS; treat the documented desktop 60 FPS target and mobile 30 FPS floor
    as human-profile acceptance. Do not hide actors or simplify gameplay in test mode.
  </action>
  <acceptance_criteria>
    A developer can reproduce the profile with one documented command; the supported reference
    desktop targets 60 FPS and mobile landscape stays at or above 30 FPS with 96 actors.
  </acceptance_criteria>
  <verify>`npm run build`; run the performance test/profile twice; compare results and record them.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve living-world feel and measured performance</name>
  <what-built>
    Purposeful Washington crowd, routed/occluded/difficulty-scaled Mitch, exact pause semantics,
    and maximum-crowd measurements.
  </what-built>
  <how-to-verify>
    Play fixed-seed rounds 1, 6, 12, and 25; watch one minute of round 1; pause manually and by
    changing tabs; review `docs/PERFORMANCE.md` and the recorded production profile.
  </how-to-verify>
  <resume-signal>
    Owner confirms early rounds feel findable, late escalation is funny, crowd feels alive, and the
    measured renderer is acceptable—or requests targeted tuning.
  </resume-signal>
</task>

</tasks>

<verification>
Run all checks and browser suites, then the human checkpoint. Confirm no broad renderer refactor or
new runtime dependency was introduced and `git diff --check` passes.
</verification>

<success_criteria>
The living simulation is time-correct, performant at the documented actor ceiling, visibly richer
than drift, and ready to receive final art/cutscenes.
</success_criteria>

<must_haves>
  <truths>
    LOOP-08 and QUAL-03 hold. Decisions D2-09 and D2-10 are verified. Pause freezes every clock and
    performance evidence, not intuition, supports SVG.
  </truths>
  <artifacts>
    Central clock/visibility code, pause tests, performance scenario, and `docs/PERFORMANCE.md`
    exist.
  </artifacts>
  <key_links>
    Visibility/manual events feed pause state; controller gates the one simulation clock; world and
    cutscene time derive from that clock; performance tests run the production renderer.
  </key_links>
</must_haves>

<threat_model>
Visibility events are untrusted lifecycle input but carry no data. Guard idempotent transitions and
clamp all deltas to prevent denial-like update loops after suspension. Performance debug parameters
remain bounded and local.
</threat_model>
