---
phase: 02-living-world-and-difficulty
plan: "01"
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - src/game/controller.ts
  - src/world/actor.ts
  - src/world/behaviors.ts
  - src/world/path-network.ts
  - src/world/scene.ts
  - src/scenes/washington.ts
  - src/render/stage-renderer.ts
  - tests/unit/path-network.test.ts
  - tests/unit/behaviors.test.ts
autonomous: true
requirements:
  - WORLD-04
---

<objective>
Replace placeholder drift with a deterministic actor simulation whose Washington crowd walks to
destinations, queues, chats, sits, uses props, and reacts while preserving a small direct runtime.
</objective>

<execution_context>
Read `02-CONTEXT.md`, `02-RESEARCH.md`, `02-UI-SPEC.md`, `docs/TECHNICAL-SPEC.md` sections
10–11, the current Washington/render/controller implementation, and existing tests.
</execution_context>

<tasks>

<task type="execute">
  <name>Define and validate the authored route and scene contracts</name>
  <read_first>
    `src/world/path-network.ts`, `src/world/scene.ts`, `src/scenes/washington.ts`,
    `src/core/rng.ts`, and `tests/unit/path-network.test.ts` if present.
  </read_first>
  <files>
    `src/world/path-network.ts`, `src/world/scene.ts`, `src/scenes/washington.ts`,
    `tests/unit/path-network.test.ts`
  </files>
  <action>
    Add the smallest typed contracts for route nodes, directed/bidirectional edges, depth portals,
    behavior anchors, hiding spots, static layer references, and instantiated scene data. Implement
    validation for unique IDs, valid endpoints, finite/in-bounds coordinates, and reachable
    authored destinations. Encode Washington sidewalks, entrances, benches, queue slots, chat
    points, crossings, and depth transitions as authored data. Keep SVG composition inside the
    scene module rather than serializing markup into generic data.
  </action>
  <acceptance_criteria>
    Washington validates with a connected usable actor graph; malformed fixtures fail with a
    focused message; core runtime code needs no Washington-specific path conditional.
  </acceptance_criteria>
  <verify>`npm run test:run -- path-network`; `npm run typecheck`.</verify>
</task>

<task type="execute">
  <name>Implement seeded actor routines and reactions</name>
  <read_first>
    `02-CONTEXT.md` Behavior Families; `src/world/actor.ts`, `src/world/behaviors.ts`,
    `src/core/rng.ts`, and `tests/unit/behaviors.test.ts` if present.
  </read_first>
  <files>`src/world/actor.ts`, `src/world/behaviors.ts`, `tests/unit/behaviors.test.ts`</files>
  <action>
    Model actors as preallocated typed records with stable IDs, role, route cursor, elapsed routine
    time, speed, pose, facing, and depth lane. Implement commuter, conversation, queue, sitter, prop
    user, and short wrong-click reaction routines as discrete state transitions driven by a named
    crowd RNG. Walking interpolates route segments by delta time. Preserve the previous routine
    across a temporary reaction. Do not allocate DOM or choose random values per frame. Unit-test
    representative transitions, queue advancement, pause-neutral updates, seed determinism, and
    segment endpoint clamping.
  </action>
  <acceptance_criteria>
    A fixed seed yields the same actor roles and decisions; all six behavior families can execute;
    no actor wanders outside valid authored routes.
  </acceptance_criteria>
  <verify>`npm run test:run -- behaviors`; search the world code for `Math.random`.</verify>
</task>

<task type="execute">
  <name>Render the living Washington crowd with stable depth</name>
  <read_first>
    `src/render/stage-renderer.ts`, `src/scenes/washington.ts`, `src/game/controller.ts`,
    `02-UI-SPEC.md` Motion/Depth sections.
  </read_first>
  <files>`src/render/stage-renderer.ts`, `src/scenes/washington.ts`, `src/game/controller.ts`</files>
  <action>
    Construct actor SVG groups once at round creation, then apply root transforms, facing, pose
    classes, and visibility in a batched render pass. Use discrete authored depth lanes and move a
    group between persistent layers only at route portals. Add subtle role-specific pose changes
    and click-local reactions without filters or full-stage effects. Keep static objects untouched
    inside the frame loop. Expose actor count and behavior state in explicit debug output only.
  </action>
  <acceptance_criteria>
    Watching a seeded round shows purposeful independent routines, correct near/far overlap, no
    actor DOM growth over time, and no change to click accounting or target behavior.
  </acceptance_criteria>
  <verify>
    Run a five-minute round, inspect DOM node count and console, watch at least one example of each
    behavior through a debug seed, and rerun Phase 1 E2E.
  </verify>
</task>

</tasks>

<verification>
Run static/unit/E2E checks. With seed 324001, record the actor role/decision trace twice and compare
it. Visually confirm at least four behaviors are narratable and actors cross depth only at authored
portals.
</verification>

<success_criteria>
The scene feels inhabited by agents with destinations and routines rather than random moving
decoration, while the implementation remains simple data plus one update/render loop.
</success_criteria>

<must_haves>
  <truths>
    WORLD-04 and decisions D2-01 through D2-03 are satisfied. Crowd choices are deterministic and
    steady-state frames allocate no DOM.
  </truths>
  <artifacts>
    Route, scene, actor, and behavior modules plus focused contract/behavior tests exist.
  </artifacts>
  <key_links>
    Washington supplies authored anchors to generic behaviors; controller advances actor records;
    renderer applies actor snapshots to pooled SVG groups.
  </key_links>
</must_haves>

<threat_model>
All scene data is bundled and trusted. Validate numeric scene data during construction to prevent
NaN transforms or runaway loops. Debug output must never interpolate URL input as markup.
</threat_model>
