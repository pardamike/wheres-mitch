---
phase: 05-release-polish-and-deployment
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - public/_headers
  - scripts/build.mjs
  - scripts/package-release.mjs
  - package.json
  - docs/DEPLOYMENT.md
  - docs/RELEASE-CHECKLIST.md
  - README.md
autonomous: false
user_setup:
  - service: Cloudflare Pages
    required_for: Optional preview and production deployment
    instructions: Authenticate only the owner's personal Cloudflare account and choose a personal project/domain.
  - service: AWS
    required_for: Optional S3 or CloudFront deployment
    instructions: Use a personal least-privilege profile scoped to the explicit bucket/distribution.
requirements:
  - DIST-03
  - DIST-04
---

<objective>
Package the accepted immutable artifact and make Cloudflare Pages, S3, optional CloudFront, preview,
production, and rollback reproducible without changing the game between hosts.
</objective>

<execution_context>
Read all of `docs/DEPLOYMENT.md`, `05-CONTEXT.md`, `05-RESEARCH.md`, accepted
`docs/TEST-RESULTS.md`, current build/public headers/package scripts, README, and the repository
license. Deployment to a real personal account/domain requires the owner's explicit credentials and
approval; never use an Adelar account, identity, token, domain, or organization.
</execution_context>

<tasks>

<task type="execute">
  <name>Finalize static security headers and deterministic release packaging</name>
  <read_first>
    `public/_headers`, `scripts/build.mjs`, `scripts/package-release.mjs`, `package.json`,
    `docs/DEPLOYMENT.md` Artifact/Security sections.
  </read_first>
  <files>
    `public/_headers`, `scripts/build.mjs`, `scripts/package-release.mjs`, `package.json`
  </files>
  <action>
    Configure feature-appropriate CSP, nosniff, referrer policy, permissions policy, frame
    ancestors, and other supported static response headers without allowing remote script/style or
    breaking the built game. Keep file-origin operation independent of headers. Implement package
    script that runs the accepted verification, builds once, creates a reproducible versioned ZIP
    from `dist` contents, emits SHA-256, lists files/sizes, and refuses to package a dirty/generated
    mismatch or unsafe path. Do not include source, `.git`, maps, tests, tokens, or environment files.
  </action>
  <acceptance_criteria>
    Unzipping the artifact reproduces the tested `dist` bytes; checksum verifies; hosted headers
    pass browser inspection; direct-file behavior remains unchanged.
  </acceptance_criteria>
  <verify>`npm run package`; checksum verification; unzip to a temporary directory and run smoke.</verify>
</task>

<task type="execute">
  <name>Rehearse Cloudflare Pages preview, production, and rollback</name>
  <read_first>
    `docs/DEPLOYMENT.md` Cloudflare sections; `docs/RELEASE-CHECKLIST.md` if present;
    `.github` workflows if present; accepted artifact metadata.
  </read_first>
  <files>`docs/DEPLOYMENT.md`, `docs/RELEASE-CHECKLIST.md`, `README.md`</files>
  <action>
    Ensure docs provide both Git integration (`npm ci && npm run build`, output `dist`) and direct
    upload of the verified directory, Node 24 environment, headers validation, custom-domain/DNS
    notes, preview review, production promotion, and rollback to an identified prior deployment or
    artifact. If the owner supplies the personal Cloudflare account and authorizes deployment,
    deploy preview first, rerun smoke/no-network checks, record URL/deployment ID, obtain production
    confirmation, then promote. Otherwise complete a command/config dry run and mark external steps
    explicitly pending—never authenticate with the current Adelar identity.
  </action>
  <acceptance_criteria>
    Another person can reproduce Cloudflare deployment/rollback from the docs; any actual public
    deployment is the accepted bytes and uses only the owner's personal account.
  </acceptance_criteria>
  <verify>Follow the documented preview checklist or dry run line by line and record evidence.</verify>
</task>

<task type="execute">
  <name>Rehearse S3 and optional CloudFront deployment/rollback</name>
  <read_first>
    `docs/DEPLOYMENT.md` S3/CloudFront sections; `docs/RELEASE-CHECKLIST.md`; release ZIP manifest.
  </read_first>
  <files>`docs/DEPLOYMENT.md`, `docs/RELEASE-CHECKLIST.md`, `README.md`</files>
  <action>
    Document bucket/static-site or private-origin choice, index/error files, correct MIME types,
    cache-control, scoped sync command, HTTPS limitation of S3 website endpoints, optional
    CloudFront/OAC/custom-domain/certificate/response-header configuration, invalidation/versioning,
    smoke checks, and rollback from a prior versioned artifact. Commands must use explicit placeholder
    bucket/distribution variables and a dry-run first; never use broad delete targets. If personal
    AWS access and explicit deployment approval are absent, do not mutate AWS.
  </action>
  <acceptance_criteria>
    The same ZIP/dist bytes can be deployed with correct types and rolled back safely; documentation
    clearly distinguishes HTTP S3 hosting from optional HTTPS CloudFront.
  </acceptance_criteria>
  <verify>Perform local command validation and, only if authorized, a scoped preview deployment smoke.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve release package and personal-account deployment handoff</name>
  <what-built>
    Verified ZIP/checksum, security headers, Cloudflare primary recipe, S3/CloudFront alternative,
    release checklist, and rollback instructions; optionally a personal-account preview.
  </what-built>
  <how-to-verify>
    Verify checksum, unpack and play locally, inspect artifact file list, walk both host recipes,
    review any preview URL, confirm account/author/domain are personal, and rehearse selecting the
    immediately prior artifact for rollback.
  </how-to-verify>
  <resume-signal>
    Owner approves package/docs and separately authorizes any desired production promotion.
  </resume-signal>
</task>

</tasks>

<verification>
From a clean clone with personal identity: `npm ci`, `npm run verify`, `npm run package`, checksum,
unzip/file smoke, hosted preview smoke if authorized, header inspection, and rollback checklist.
Confirm Git log/remote/CLI identity contain no Adelar association before any push.
</verification>

<success_criteria>
One accepted static artifact is safely packageable, deployable, and reversible on the preferred and
alternate hosts without a server, account mix-up, or host-specific code fork.
</success_criteria>

<must_haves>
  <truths>
    DIST-03 and DIST-04 hold. Decisions D5-07 and D5-08 are reproducible, and all identity/account
    actions honor HANDOFF.md.
  </truths>
  <artifacts>
    Static headers, deterministic package script, release ZIP/checksum, deployment guide, release
    checklist, and updated README exist.
  </artifacts>
  <key_links>
    Package command consumes the already verified build; both host recipes publish those bytes;
    smoke tests validate the deployed artifact; rollback identifies a prior immutable artifact.
  </key_links>
</must_haves>

<threat_model>
Never embed cloud credentials or use organizational accounts. Scope upload/delete commands to an
explicit validated bucket/prefix and use dry-run. CSP/headers block unnecessary capabilities, while
the app remains safe without headers under file origin. Do not deploy without owner authorization.
</threat_model>
