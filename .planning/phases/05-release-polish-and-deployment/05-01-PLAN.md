---
phase: 05-release-polish-and-deployment
plan: "01"
type: execute
wave: 1
depends_on:
  - 04-03
files_modified:
  - src/storage/schema.ts
  - src/storage/storage.ts
  - src/game/records.ts
  - src/game/controller.ts
  - src/render/ui-renderer.ts
  - src/render/stage-renderer.ts
  - public/index.html
  - public/styles.css
  - tests/unit/storage.test.ts
  - tests/e2e/persistence.spec.ts
  - tests/e2e/responsive-and-motion.spec.ts
autonomous: false
requirements:
  - UI-05
  - UI-06
  - SAVE-01
  - SAVE-02
---

<objective>
Finish responsive/reduced-motion behavior and safely persist the tiny allowed record/settings schema
without ever making browser storage a prerequisite for play.
</objective>

<execution_context>
Read `05-CONTEXT.md`, `05-RESEARCH.md`, `05-UI-SPEC.md`, `docs/UI-SPEC.md`, storage/motion sections
of `docs/TECHNICAL-SPEC.md`, `docs/TEST-PLAN.md`, current records/controller/render/CSS, and every
existing supported-view screenshot.
</execution_context>

<tasks>

<task type="execute">
  <name>Implement a validated versioned optional storage adapter</name>
  <read_first>
    `src/storage/schema.ts`, `src/storage/storage.ts`, `src/game/records.ts`,
    `src/game/controller.ts`, `tests/unit/storage.test.ts`, and
    `tests/e2e/persistence.spec.ts` if present.
  </read_first>
  <files>
    `src/storage/schema.ts`, `src/storage/storage.ts`, `src/game/records.ts`,
    `src/game/controller.ts`, `tests/unit/storage.test.ts`, `tests/e2e/persistence.spec.ts`
  </files>
  <action>
    Define one v1 schema and project-specific key containing only best completed rounds, fastest
    valid find, lifetime catches, sound preference, and motion override. Parse persisted JSON as
    unknown, validate field types/enums, clamp finite numeric records, and reject incompatible
    versions to defaults. Wrap localStorage acquisition/read/write/remove separately; on any error,
    switch to the in-memory adapter and report a nonfatal availability flag to settings UI. Write
    only after semantic record/setting changes, never per frame. Add confirmed Reset Local Records.
    Unit-test missing/valid/corrupt/wrong-version/bad-field/extreme-value/read-error/write-error/quota
    paths and record update rules. Add browser coverage for valid reload, reset, and a storage API
    that throws while the complete game loop continues.
  </action>
  <acceptance_criteria>
    Valid records/settings reload when storage is available; every failure mode starts and completes
    play with safe in-memory values; no unrelated or personal data is written.
  </acceptance_criteria>
  <verify>`npm run test:run -- storage records`; inspect browser storage after a run.</verify>
</task>

<task type="execute">
  <name>Complete responsive shell, settings, keyboard focus, and live status</name>
  <read_first>
    `05-UI-SPEC.md` Responsive/Settings/Keyboard sections; `public/index.html`;
    `public/styles.css`; `src/render/ui-renderer.ts`; existing controls/dialogs.
  </read_first>
  <files>
    `public/index.html`, `public/styles.css`, `src/render/ui-renderer.ts`,
    `tests/e2e/responsive-and-motion.spec.ts`
  </files>
  <action>
    Implement documented desktop/tablet/667×375 landscape layouts, safe-area padding, stage
    letterboxing, compact/two-row HUD, 44px touch targets, and a friendly portrait rotate panel.
    Add Sound and Motion controls plus Records with em-dash empty values and session-only label on
    storage failure. Ensure dialogs move/return focus, Escape precedence is correct, controls have
    visible focus, outcome status uses a polite live region, decorative SVG is hidden from the
    accessibility tree, and 200% zoom content stays reachable. Do not add an accessibility library.
  </action>
  <acceptance_criteria>
    Title, stage, HUD, pause/settings, outcomes, and records are usable at all specified viewports,
    by keyboard for controls, and at 200% zoom without covering search geometry.
  </acceptance_criteria>
  <verify>
    Playwright viewport/keyboard assertions plus manual Chrome/Safari checks at every release
    viewport in `05-UI-SPEC.md`.
  </verify>
</task>

<task type="execute">
  <name>Honor system and saved reduced-motion choices without changing rules</name>
  <read_first>
    `05-UI-SPEC.md` Reduced Motion; `src/render/stage-renderer.ts`,
    `src/render/ui-renderer.ts`, cutscene/ambient modules, and
    `tests/e2e/responsive-and-motion.spec.ts` if present.
  </read_first>
  <files>
    `src/render/stage-renderer.ts`, `src/render/ui-renderer.ts`, `src/game/controller.ts`,
    `public/styles.css`, `tests/e2e/responsive-and-motion.spec.ts`
  </files>
  <action>
    Resolve effective motion from `system`, `reduce`, or `full`, with `matchMedia` updates only while
    mode is system. Stop decorative loops and simplify pose effects in reduced mode; use shortened
    capture/escape sequences already defined; retain essential Mitch routing, exposures, attempts,
    and state messages. Ensure changing the setting during pause/play applies coherently and saves
    when possible. Test system preference, all overrides, reload, equivalent outcomes, and absence
    of indefinite animation classes in reduced mode.
  </action>
  <acceptance_criteria>
    Reduced motion materially reduces ambient/cutscene travel while preserving identical game
    state and all required narrative beats; player override wins over system and persists safely.
  </acceptance_criteria>
  <verify>`npm run test:e2e -- responsive-and-motion persistence`; manual motion comparison.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve release layouts, records, and reduced motion</name>
  <what-built>
    Versioned local records/settings, failure fallback, all release layouts, rotate panel, keyboard
    focus behavior, live status, and full/reduced/system motion modes.
  </what-built>
  <how-to-verify>
    Complete/reload a run, reset records, simulate blocked storage, test keyboard controls and 200%
    zoom, then play both outcomes at 1440×900, 1024×768, 667×375, and reduced motion.
  </how-to-verify>
  <resume-signal>Owner approves release UX or lists specific corrections.</resume-signal>
</task>

</tasks>

<verification>
Run all static/unit/E2E/file checks, the human checkpoint, and manually inspect storage contents to
confirm only the documented record exists. Run `git diff --check`.
</verification>

<success_criteria>
The complete game is considerate and usable across target layouts/motion preferences, while records
are helpful when available and invisible to core reliability when unavailable.
</success_criteria>

<must_haves>
  <truths>
    UI-05, UI-06, SAVE-01, and SAVE-02 hold. Decisions D5-01, D5-02, D5-04, and D5-05 are verified.
  </truths>
  <artifacts>
    Schema/storage adapter, integrated settings/records UI, responsive/reduced CSS/rendering, and
    focused unit/browser tests exist.
  </artifacts>
  <key_links>
    Controller loads adapter defaults once; semantic changes persist; UI reports adapter
    availability; effective motion configures ambient and sequence renderers without altering rules.
  </key_links>
</must_haves>

<threat_model>
Treat storage bytes as untrusted and never insert them as HTML. Bound values and catch API failures.
No personal identifiers, gameplay history stream, analytics, or cross-origin storage is collected.
</threat_model>
