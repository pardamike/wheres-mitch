---
phase: 05-release-polish-and-deployment
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - playwright.config.ts
  - tests/e2e/game-loop.spec.ts
  - tests/e2e/scene-deck.spec.ts
  - tests/e2e/persistence.spec.ts
  - tests/e2e/responsive-and-motion.spec.ts
  - tests/e2e/file-smoke.spec.ts
  - tests/e2e/network.spec.ts
  - tests/e2e/visual.spec.ts
  - tests/fixtures/seeds.ts
  - scripts/verify-dist.mjs
  - package.json
  - docs/TEST-RESULTS.md
autonomous: false
requirements:
  - DIST-02
  - QUAL-02
  - QUAL-04
---

<objective>
Qualify the actual production artifact across all game paths, scenes, input/motion/storage modes,
supported desktop browsers, and direct `file://` execution with zero runtime network dependency.
</objective>

<execution_context>
Read all of `docs/TEST-PLAN.md`, `docs/TECHNICAL-SPEC.md` build/debug/failure sections,
`05-CONTEXT.md`, `05-RESEARCH.md`, current tests/config/build output, and browser support notes.
Do not weaken product behavior for tests or replace semantic assertions with implementation detail.
</execution_context>

<tasks>

<task type="execute">
  <name>Consolidate the deterministic product-critical E2E matrix</name>
  <read_first>
    All `tests/e2e/*.spec.ts`, `tests/fixtures/seeds.ts`, `playwright.config.ts`, and
    `docs/TEST-PLAN.md` E2E scenarios.
  </read_first>
  <files>
    `playwright.config.ts`, `tests/e2e/game-loop.spec.ts`, `tests/e2e/scene-deck.spec.ts`,
    `tests/e2e/persistence.spec.ts`, `tests/e2e/responsive-and-motion.spec.ts`,
    `tests/e2e/network.spec.ts`, `tests/e2e/visual.spec.ts`, `tests/fixtures/seeds.ts`
  </files>
  <action>
    Remove duplicated setup/assertions while retaining meaningful coverage. Centralize only stable
    seed/launch helpers, not business logic. Ensure suite covers last-click capture, ten-miss escape,
    one-time outcome locks, restart, records/settings persistence and failure, mute/pause, reduced
    motion, touch, each scene, shuffle behavior, responsive layouts, and reviewed visuals. Fail on
    page/console errors and all post-load network requests. Run against the production build. Keep
    selectors accessible/semantic and waits state-based. Avoid tests of trivial browser rendering.
  </action>
  <acceptance_criteria>
    The requirement matrix in `docs/TEST-PLAN.md` maps to a named useful test without duplicate
    low-value cases or arbitrary sleeps; all run reproducibly with canonical seeds.
  </acceptance_criteria>
  <verify>`npm run build`; `npm run test:e2e` repeatedly, including touch/mobile project.</verify>
</task>

<task type="execute">
  <name>Prove direct-file execution and immutable artifact invariants</name>
  <read_first>
    `tests/e2e/file-smoke.spec.ts`, `scripts/verify-dist.mjs`, `scripts/build.mjs`,
    `docs/TECHNICAL-SPEC.md` Build Contract, and `docs/TEST-PLAN.md` Direct File.
  </read_first>
  <files>
    `tests/e2e/file-smoke.spec.ts`, `scripts/verify-dist.mjs`, `package.json`
  </files>
  <action>
    Add a post-build verifier for allowed files, relative references, classic IIFE, forbidden remote
    URL/import/fetch/worker patterns, expected content types by extension, and no source-map/source
    leakage in production. Navigate Playwright to an absolute encoded `file://` URL and exercise
    title/start, miss, catch/next round, ten-miss escape/restart, and settings fallback without a
    server. Capture any console/page error. Run direct-file tests in each supported desktop browser
    where Playwright supports the engine, with a documented manual Safari file smoke.
  </action>
  <acceptance_criteria>
    Double-clicked `dist/index.html` runs the complete loop with no error or remote request in
    Chrome, Edge, Firefox, and Safari smoke coverage; artifact verifier blocks origin-only features.
  </acceptance_criteria>
  <verify>`npm run build`; `npm run test:file`; manually double-click in stable Safari/Edge.</verify>
</task>

<task type="execute">
  <name>Execute and record the supported-browser release smoke matrix</name>
  <read_first>
    `docs/TEST-PLAN.md` Browser Matrix/Release Gate; `docs/PERFORMANCE.md`;
    `docs/TEST-RESULTS.md` if present; full production build.
  </read_first>
  <files>`docs/TEST-RESULTS.md`, `package.json`</files>
  <action>
    Run typecheck, lint, format check, unit, build verifier, Chromium/Firefox/WebKit E2E, direct-file,
    visual, and maximum-crowd scenarios. Manually smoke current stable Chrome, Edge, Firefox, and
    Safari with mouse/keyboard plus one available real mobile landscape browser. Record date, OS,
    exact browser versions, commands, pass/fail, known limitations, performance measurements, and
    any approved visual-baseline changes in `docs/TEST-RESULTS.md`. Fix product defects in owning
    modules; do not add test-only app branches or conceal failures.
  </action>
  <acceptance_criteria>
    Every supported desktop browser completes both outcomes and direct-file startup with no uncaught
    error; all automated release commands pass; remaining limitations are nonblocking and explicit.
  </acceptance_criteria>
  <verify>`npm run verify`; follow the manual matrix and sign each row in `docs/TEST-RESULTS.md`.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Accept the release candidate artifact</name>
  <what-built>
    One verified `dist/` release candidate with full deterministic E2E, direct-file, no-network,
    visual, performance, accessibility, and browser-matrix evidence.
  </what-built>
  <how-to-verify>
    Review `docs/TEST-RESULTS.md`, open the exact candidate both directly and through the local HTTP
    server, play both outcomes and several rounds, and inspect network/console. Confirm no known
    issue blocks a public static release.
  </how-to-verify>
  <resume-signal>Owner explicitly accepts the artifact for packaging/deployment.</resume-signal>
</task>

</tasks>

<verification>
`npm run verify` passes from a clean `npm ci`; manual matrix and owner checkpoint are complete;
`git status` contains no accidental reports/videos/traces unless intentionally retained.
</verification>

<success_criteria>
The exact static product has reproducible evidence for every business-critical path and supported
runtime, including true no-server play.
</success_criteria>

<must_haves>
  <truths>
    DIST-02, QUAL-02, and QUAL-04 hold. Decisions D5-03, D5-06, and D5-09 are verified against the
    production artifact.
  </truths>
  <artifacts>
    Complete consolidated E2E suite, direct-file tests, artifact verifier, canonical fixtures, and
    signed `docs/TEST-RESULTS.md` exist.
  </artifacts>
  <key_links>
    Verify command builds once and tests that artifact; browser tests use canonical seeds/semantic
    state; direct-file tests point at the same `dist`; results record the tested artifact/build.
  </key_links>
</must_haves>

<threat_model>
Tests must not leak local paths/tokens into committed snapshots or upload traces externally. The
artifact verifier blocks remote execution/collection primitives. Browser storage is cleared or
uses synthetic records only; no personal data enters results.
</threat_model>

