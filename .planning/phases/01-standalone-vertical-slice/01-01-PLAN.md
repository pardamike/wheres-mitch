---
phase: 01-standalone-vertical-slice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - tsconfig.json
  - eslint.config.js
  - .prettierrc.json
  - scripts/build.mjs
  - scripts/dev.mjs
  - src/main.ts
  - public/index.html
  - public/styles.css
  - public/favicon.svg
  - public/404.html
  - public/_headers
  - tests/unit/build-contract.test.ts
autonomous: true
requirements:
  - DIST-01
  - DIST-05
---

<objective>
Create the development toolchain and the exact static artifact contract on which every later plan
depends. At completion, a minimal title shell builds into a relative-path, classic-IIFE `dist/`
that opens directly and contains no network behavior.
</objective>

<execution_context>
Read `AGENTS.md`, `HANDOFF.md`, `docs/TECHNICAL-SPEC.md` sections 2–6, `docs/TEST-PLAN.md`
sections 2–5, `01-CONTEXT.md`, and `01-RESEARCH.md`. Before any install, commit, or push, verify
personal Git identity exactly as required by `HANDOFF.md`. This plan may create a local commit only
after that gate passes on the personal machine.
</execution_context>

<tasks>

<task type="execute">
  <name>Initialize the pinned TypeScript development toolchain</name>
  <read_first>
    `AGENTS.md`; `docs/TECHNICAL-SPEC.md`; existing `package.json`, `.nvmrc`, `tsconfig.json`,
    `eslint.config.js`, and `.prettierrc.json` if present.
  </read_first>
  <files>
    `package.json`, `package-lock.json`, `.nvmrc`, `tsconfig.json`, `eslint.config.js`,
    `.prettierrc.json`
  </files>
  <action>
    Pin Node 24 LTS and npm-compatible engines. Add only development dependencies documented in
    the technical specification: TypeScript, esbuild, Vitest, Playwright, ESLint with TypeScript
    support, and Prettier. Define the complete command contract, allowing commands whose files land
    in later plans to fail clearly until implemented. Configure strict TypeScript with DOM libs,
    ES2020 browser output assumptions, `noEmit`, and no unchecked implicit types. Configure ESLint
    for correctness and Prettier for source/config/docs without broad custom style rules. Generate
    and retain the lockfile; leave `dependencies` empty or absent.
  </action>
  <acceptance_criteria>
    `npm ci` succeeds under Node 24; `npm ls --omit=dev` shows no production dependency; the
    package contains the documented scripts; `npm run typecheck` can execute once source exists.
  </acceptance_criteria>
  <verify>
    `node --version`; `npm ci`; `npm ls --omit=dev`; inspect `package.json` and lockfile diff.
  </verify>
</task>

<task type="execute">
  <name>Build a safe classic-IIFE static artifact and local development loop</name>
  <read_first>
    `docs/TECHNICAL-SPEC.md` sections 4–6; `scripts/build.mjs`, `scripts/dev.mjs`, `src/main.ts`,
    `public/index.html`, and `public/styles.css` if present.
  </read_first>
  <files>
    `scripts/build.mjs`, `scripts/dev.mjs`, `src/main.ts`, `public/index.html`, `public/styles.css`,
    `public/favicon.svg`, `public/404.html`, `public/_headers`
  </files>
  <action>
    Implement the build using the esbuild JavaScript API with `bundle`, browser platform, IIFE
    format, and ES2020 target. Resolve repository and `dist` paths and refuse cleanup unless the
    target is the exact repository child. Copy only the approved public files. Scan emitted source
    for module scripts, unresolved imports, remote URLs, absolute-root asset paths, and references
    outside `dist`; report file byte sizes. Implement a tiny Node-built-in development server and
    rebuild/watch loop that is not imported by production. Add a minimal typed `src/main.ts` that
    marks the shell ready without game logic. Create semantic title-shell HTML with relative
    `./styles.css` and deferred `./game.js`; include no remote font, image, or script.
  </action>
  <acceptance_criteria>
    `npm run build` emits the documented files; `dist/index.html` references only relative local
    assets and no module script; double-clicking it displays the shell with no console or network
    error; repeated builds do not touch files outside `dist`.
  </acceptance_criteria>
  <verify>
    `npm run build`; list and inspect `dist`; open `dist/index.html` directly in a browser; run the
    dev command and confirm a local HTTP load.
  </verify>
</task>

<task type="execute">
  <name>Encode the build and privacy invariants as useful tests</name>
  <read_first>
    `docs/TEST-PLAN.md` Build Invariants; `scripts/build.mjs`; `public/index.html`;
    `tests/unit/build-contract.test.ts` if present.
  </read_first>
  <files>`tests/unit/build-contract.test.ts`, `package.json`</files>
  <action>
    Add tests that build into an isolated known output and assert relative HTML references,
    classic-script format, no unresolved import/require/dynamic import, no HTTP(S) literals in
    runtime artifacts, and the expected entry files. Test the cleanup guard with a deliberately
    invalid target without deleting any real path. Keep assertions about product invariants, not
    esbuild internals or minified bytes.
  </action>
  <acceptance_criteria>
    The test fails if a remote URL, module script, runtime import, or unsafe output target is
    introduced, and passes for the production build.
  </acceptance_criteria>
  <verify>`npm run test:run`; `npm run build`; `npm run format:check`.</verify>
</task>

</tasks>

<verification>
Run `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:run`, and
`npm run build`. Load both the development URL and direct `dist/index.html`; inspect console and
network panels. Confirm `git diff --check` and confirm no runtime dependency was added.
</verification>

<success_criteria>
The repository has a reproducible Node 24 toolchain and produces a safe, relative, runtime-free IIFE
artifact whose shell works with and without a server.
</success_criteria>

<must_haves>
  <truths>
    DIST-01 and DIST-05 are enforced by the artifact, not merely documented. Build-output
    protection supplies part of the later QUAL-01 verification. Decisions D1-01, D1-02, and D1-08
    remain true.
  </truths>
  <artifacts>
    `package-lock.json`, `scripts/build.mjs`, `public/index.html`, and
    `tests/unit/build-contract.test.ts` exist and pass.
  </artifacts>
  <key_links>
    `package.json` scripts call the checked-in scripts; `public/index.html` links `./styles.css`
    and `./game.js`; build tests validate the same output `npm run build` creates.
  </key_links>
</must_haves>

<threat_model>
The game accepts no remote/user content. Main risks are accidental remote dependencies and an unsafe
clean step. Contain both with an allowlisted copy set, exact-path deletion guard, artifact scanning,
and tests. Do not add secrets or environment variables to client code.
</threat_model>
