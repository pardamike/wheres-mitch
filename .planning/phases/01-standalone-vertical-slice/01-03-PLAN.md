---
phase: 01-standalone-vertical-slice
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - public/index.html
  - public/styles.css
  - src/main.ts
  - src/game/controller.ts
  - src/game/input.ts
  - src/render/svg-dom.ts
  - src/render/stage-renderer.ts
  - src/render/ui-renderer.ts
  - src/scenes/washington.ts
  - playwright.config.ts
  - tests/e2e/vertical-slice.spec.ts
  - tests/e2e/file-smoke.spec.ts
autonomous: false
requirements:
  - LOOP-01
  - MITCH-01
  - WORLD-01
  - UI-01
  - UI-02
  - UI-03
  - CONT-01
---

<objective>
Connect the proven rules to a complete, visually coherent Washington SVG slice that a human can
start, search, catch, lose, and restart through the actual browser UI.
</objective>

<execution_context>
Read `docs/GAME-DESIGN.md`, `docs/UI-SPEC.md`, `docs/ART-BIBLE.md`, `docs/TEST-PLAN.md`,
`01-UI-SPEC.md`, all completed Phase 1 source, and every file listed below before editing it.
</execution_context>

<tasks>

<task type="execute">
  <name>Author the title, HUD, overlays, and Washington SVG prototype</name>
  <read_first>
    `01-UI-SPEC.md`; `docs/ART-BIBLE.md`; `public/index.html`; `public/styles.css`;
    `src/scenes/washington.ts`; `src/render/svg-dom.ts`; `src/render/stage-renderer.ts` if present.
  </read_first>
  <files>
    `public/index.html`, `public/styles.css`, `src/scenes/washington.ts`, `src/render/svg-dom.ts`,
    `src/render/stage-renderer.ts`
  </files>
  <action>
    Implement the approved semantic HTML surfaces and an inline SVG with logical view box
    1440×900. Build an original Washington street silhouette with Capitol/civic cues, layered
    sidewalks, props, a small animated placeholder crowd, foreground occluders, and an unmistakable
    original turtle-bodied Mitch caricature. Construct persistent layer groups once; mark
    decorative SVG inaccessible. Give the complete visible target group the stable semantic target
    marker and an appropriately generous Phase 1 hit shape. Implement miss markers in the effects
    layer. Use the canonical palette/tokens and visible disclaimer/copy; use no remote assets.
  </action>
  <acceptance_criteria>
    Title and gameplay match the phase wireframe, Washington is recognizable, Mitch reads at game
    scale, no protected hidden-object franchise design is copied, and layouts work at 1440×900,
    1024×768, and 667×375 landscape.
  </acceptance_criteria>
  <verify>
    Load the dev build at all three viewports, inspect SVG layers, keyboard-tab through controls,
    and perform a human originality/disclaimer check.
  </verify>
</task>

<task type="execute">
  <name>Wire input, reducer, controller, and immediate UI feedback</name>
  <read_first>
    `docs/TECHNICAL-SPEC.md` Runtime/Input sections; `src/main.ts`, `src/game/controller.ts`,
    `src/game/input.ts`, `src/render/ui-renderer.ts`, and Phase 1 core modules if present.
  </read_first>
  <files>
    `src/main.ts`, `src/game/controller.ts`, `src/game/input.ts`, `src/render/stage-renderer.ts`,
    `src/render/ui-renderer.ts`
  </files>
  <action>
    Wire one stage `pointerdown` boundary accepting only the primary pointer/primary button. Resolve
    target activation from the composed path; otherwise transform the pointer only for miss-marker
    placement. Stop propagation from all controls. Render mode, round, clicks, caught, and best
    immediately from state. Add temporary deterministic capture/escape sequences that dispatch
    completion once and respect current outcome tokens. Start the Search and Search Again work through labeled
    controls. Add simple movement through the single animation loop, but leave advanced routed AI
    to Phase 2. Expose documented debug seed/state markers only when query debug mode is explicit.
  </action>
  <acceptance_criteria>
    Mouse and touch follow one path; every miss decrements once; visible Mitch catches through his
    group; controls never consume clicks; outcome animation cannot accept queued stage input; next
    round and restart render correct state.
  </acceptance_criteria>
  <verify>
    Manually exercise catch on click 1 and click 10, ten misses, rapid double activation, control
    clicks, touch emulation, and restart while watching the HUD and console.
  </verify>
</task>

<task type="execute">
  <name>Prove both real UI paths over HTTP and direct file</name>
  <read_first>
    `docs/TEST-PLAN.md` E2E and Direct File sections; `playwright.config.ts`;
    `tests/e2e/vertical-slice.spec.ts`; `tests/e2e/file-smoke.spec.ts` if present.
  </read_first>
  <files>
    `playwright.config.ts`, `tests/e2e/vertical-slice.spec.ts`, `tests/e2e/file-smoke.spec.ts`,
    `package.json`
  </files>
  <action>
    Configure Playwright against the built artifact and a development-only static web server. Use
    accessible names and semantic game-state markers, fixed seed 324001, and no SVG coordinate
    assumptions except intentional stage-miss points. Cover title copy/disclaimer, start, HUD ten,
    miss decrement, target capture, next-round reset, ten-miss loss, result stats, and clean
    restart. Add a direct `file://` smoke that starts and completes at least one path, fails on page
    or console errors, and confirms no remote request. Do not add test-specific gameplay behavior.
  </action>
  <acceptance_criteria>
    The vertical slice is exercised through its actual React-free browser UI under HTTP and
    direct-file execution; failures identify semantic state rather than arbitrary sleeps.
  </acceptance_criteria>
  <verify>`npm run build`; `npm run test:e2e`; `npm run test:file`.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve the first playable loop before deeper animation work</name>
  <what-built>
    A complete Washington-only static game with title/disclaimer, ten-click HUD, target, both
    outcomes, next round, restart, touch path, and direct-file artifact.
  </what-built>
  <how-to-verify>
    Run `npm ci && npm run dev`, open the printed URL, and play one caught and one ten-miss run at
    desktop and 667×375 landscape. Then run `npm run build` and double-click `dist/index.html`.
    Confirm the premise is instantly clear, ten attempts behave exactly, the target reads as Turtle
    Mitch, controls do not count as misses, and the basic visual direction is worth expanding.
  </how-to-verify>
  <resume-signal>Owner explicitly approves the vertical slice or lists requested revisions.</resume-signal>
</task>

</tasks>

<verification>
Run `npm run verify` insofar as Phase 1 commands exist, then manually execute the checkpoint. Inspect
browser network and console under HTTP and file origins. Run `git diff --check` and confirm only
task-scoped files changed.
</verification>

<success_criteria>
A human can play a coherent standalone game, and automation proves both edge outcomes and the
artifact boundary before animation complexity is added.
</success_criteria>

<must_haves>
  <truths>
    LOOP-01, MITCH-01, WORLD-01, UI-01 through UI-03, and CONT-01 are visible in the shipped UI.
    Decisions D1-04, D1-07, and D1-08 hold. The user approves the direction.
  </truths>
  <artifacts>
    A playable `dist/`, Washington scene module, controller/input/renderers, and the two browser
    test files exist.
  </artifacts>
  <key_links>
    Stage input dispatches typed reducer events; state drives HUD and overlays; Washington markup
    occupies persistent SVG layers; tests operate the same built UI the user sees.
  </key_links>
</must_haves>

<threat_model>
Raw query values never enter HTML; text is assigned with `textContent`; no remote resources or
untrusted SVG exist. Pointer filtering and synchronous state locks prevent duplicated outcomes.
Public copy remains fictional satire and avoids factual allegations.
</threat_model>
