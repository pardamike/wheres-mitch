---
phase: 03-signature-satire-and-cutscenes
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/render/cutscenes/escape.ts
  - src/render/stage-renderer.ts
  - src/render/ui-renderer.ts
  - src/game/controller.ts
  - src/audio/audio-engine.ts
  - src/audio/cues.ts
  - src/game/visibility.ts
  - tests/unit/audio-engine.test.ts
  - tests/e2e/escape-cutscene.spec.ts
  - tests/e2e/audio.spec.ts
autonomous: false
requirements:
  - OUT-03
  - OUT-04
  - AUDIO-01
  - AUDIO-02
---

<objective>
Deliver the complete absurd helicopter escape and original procedural sound layer, with proper
gesture unlock, mute/pause behavior, nonviolent framing, and delayed game-over restart.
</objective>

<execution_context>
Read `03-CONTEXT.md` Escape beats, `03-RESEARCH.md` Audio/Content, `03-UI-SPEC.md` Escape/Sound,
approved art/copy, sequence runner, clock/visibility, reducer, and current UI.
</execution_context>

<tasks>

<task type="execute">
  <name>Implement a nonfatal gesture-unlocked procedural audio engine</name>
  <read_first>
    `docs/TECHNICAL-SPEC.md` Audio Architecture; `src/audio/audio-engine.ts`,
    `src/audio/cues.ts`, `src/game/visibility.ts`, `tests/unit/audio-engine.test.ts` if present.
  </read_first>
  <files>
    `src/audio/audio-engine.ts`, `src/audio/cues.ts`, `src/game/visibility.ts`,
    `tests/unit/audio-engine.test.ts`
  </files>
  <action>
    Lazily create/resume Web Audio only inside Start or an explicit later player gesture. Implement
    a restrained master gain and original synthesized cues for UI, miss, reveal, shell, Capitol,
    cash pop, rotor, rope/winch, and result sting using oscillator/noise/envelopes. Expose unlock,
    cue, setMuted, suspend, resume, and dispose. Mute is immediate. Manual/hidden pause suspends;
    resume occurs only after prior unlock and valid gameplay resume. Catch audio errors and continue
    visually without retry loops. Use an injected fake AudioContext contract in unit tests.
  </action>
  <acceptance_criteria>
    Page load is silent and creates no audio context; Start unlocks without autoplay error; mute and
    pause work; unavailable Web Audio never blocks game state.
  </acceptance_criteria>
  <verify>`npm run test:run -- audio-engine`; browser console/audio review after fresh page load.</verify>
</task>

<task type="execute">
  <name>Stage the complete money-and-helicopter escape</name>
  <read_first>
    `03-UI-SPEC.md` Escape Sequence; `src/render/cutscenes/escape.ts`; approved Elaine,
    helicopter, money, and Mitch rigs; `src/render/stage-renderer.ts`; `src/audio/cues.ts`.
  </read_first>
  <files>
    `src/render/cutscenes/escape.ts`, `src/render/stage-renderer.ts`,
    `src/render/ui-renderer.ts`, `src/audio/cues.ts`
  </files>
  <action>
    Define fixed-fiction beats: tenth-miss freeze, three-or-more generic money bags pop around
    Mitch, helicopter with clearly visible PRC flag and Elaine in cockpit enters, rotor/wash motion,
    rope lowers, connection is communicated without harm, Mitch and bags rise, helicopter exits,
    then game-over card appears. Use the sequence runner, approved rigs, original fixed copy, and
    semantic audio cues. Build the reduced-motion equivalent with short translations/fades while
    retaining every narrative element. Allow Skip after the outcome is visually established.
  </action>
  <acceptance_criteria>
    Every required object and beat is legible, impossible/cartoon framing is obvious, sequence is
    nonviolent, and the game-over card does not accept restart before sequence resolution.
  </acceptance_criteria>
  <verify>
    Manual frame-by-frame review in normal/reduced/muted modes at desktop/mobile; public-copy audit.
  </verify>
</task>

<task type="execute">
  <name>Connect tenth miss, escape resolution, and audio browser coverage</name>
  <read_first>
    `src/game/controller.ts`, `src/core/reducer.ts`, `src/render/ui-renderer.ts`,
    `tests/e2e/escape-cutscene.spec.ts`, `tests/e2e/audio.spec.ts` if present.
  </read_first>
  <files>
    `src/game/controller.ts`, `src/render/ui-renderer.ts`,
    `tests/e2e/escape-cutscene.spec.ts`, `tests/e2e/audio.spec.ts`
  </files>
  <action>
    Start escape only after the reducer accepts the tenth miss and has synchronously locked input.
    On normal or skipped completion, dispatch one `ESCAPE_COMPLETE`; only then enable Search Again.
    Test all ten misses, one escape start, essential beat markers, final statistics/best, disabled
    early restart, and clean replay. Add browser coverage that observes no autoplay console errors,
    mute label/state, and pause suspension using a browser-safe signal rather than audio waveform
    snapshots. E2E must operate the real UI and fixed local seeds.
  </action>
  <acceptance_criteria>
    Ten misses always produce one complete escape and one game-over state; no queued click or Skip
    duplicates the sequence; sound lifecycle never controls visual completion.
  </acceptance_criteria>
  <verify>`npm run test:e2e -- escape-cutscene audio`; rerun reducer/sequence tests.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve both signature outcomes together</name>
  <what-built>
    Final Capitol-return capture, Chinese-flag helicopter/ Elaine/money escape, game-over flow,
    synthesized audio, mute, pause, Skip, and reduced-motion versions.
  </what-built>
  <how-to-verify>
    Play a catch and ten-miss loss with sound on, repeat muted, then repeat reduced motion at desktop
    and 667×375. Confirm both jokes read instantly, all required beats appear, copy makes no factual
    claim, imagery remains absurd/nonviolent, and timing feels funny rather than tedious.
  </how-to-verify>
  <resume-signal>Owner explicitly approves both outcomes and audio or lists revisions.</resume-signal>
</task>

</tasks>

<verification>
Run full static/unit/E2E/file checks available through Phase 3, complete the human checkpoint, and
rerun the asset-manifest/content audit for synthesized cues and outcome art.
</verification>

<success_criteria>
The game now has its complete signature win/loss personality, communicates both outcomes without
sound, behaves correctly with sound, and never turns the visual gag into a factual allegation.
</success_criteria>

<must_haves>
  <truths>
    OUT-03, OUT-04, AUDIO-01, and AUDIO-02 hold. Decisions D3-06 through D3-10 remain true and owner
    approval is recorded.
  </truths>
  <artifacts>
    Escape sequence, audio engine/cues, UI/controller integration, and outcome/audio tests exist.
  </artifacts>
  <key_links>
    Tenth miss locks reducer state; one outcome token starts sequence/audio cues; completion enters
    game over; visibility/mute control audio independently of game correctness.
  </key_links>
</must_haves>

<threat_model>
Audio is local/procedural and nonfatal; no microphone or media permission is requested. Outcome
copy is fixed and safe-set. Token checks prevent stale sequence completion. Content approval guards
against unsupported real-world implication and demeaning depiction.
</threat_model>
