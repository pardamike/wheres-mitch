---
phase: 03-signature-satire-and-cutscenes
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/render/cutscenes/sequence.ts
  - src/render/cutscenes/capture.ts
  - src/render/stage-renderer.ts
  - src/render/ui-renderer.ts
  - src/game/controller.ts
  - public/styles.css
  - tests/unit/sequence.test.ts
  - tests/e2e/capture-cutscene.spec.ts
autonomous: true
requirements:
  - OUT-01
  - OUT-02
---

<objective>
Implement the complete nonviolent catch choreography—from scene freeze and retraction through the
absurd Capitol delivery—and transition automatically to the next harder round exactly once.
</objective>

<execution_context>
Read `03-CONTEXT.md` Catch beats, `03-RESEARCH.md` Sequence Runner, `03-UI-SPEC.md` Catch Sequence,
the approved art rigs, controller/clock/reducer, and current capture placeholder.
</execution_context>

<tasks>

<task type="execute">
  <name>Build one pauseable, cancellable declarative sequence runner</name>
  <read_first>
    `src/render/cutscenes/sequence.ts`, `src/core/clock.ts`, `src/game/controller.ts`, and
    `tests/unit/sequence.test.ts` if present.
  </read_first>
  <files>`src/render/cutscenes/sequence.ts`, `tests/unit/sequence.test.ts`</files>
  <action>
    Implement an ordered beat runner driven by the existing simulation clock with enter/update/exit
    hooks, default and reduced durations, progress clamping, skip-to-resolution, cancellation, and
    one completion callback. It owns visual timeline data only; reducer mode remains authoritative.
    Use a monotonically increasing outcome token so a stale completion cannot advance a restarted
    run. Unit-test exact beat order, zero/large delta handling, pause, reduced durations, skip,
    cancellation, and duplicate completion suppression with a fake clock.
  </action>
  <acceptance_criteria>
    The runner never uses chained timers, respects pause, and resolves or cancels once under rapid
    restart/skip input.
  </acceptance_criteria>
  <verify>`npm run test:run -- sequence`; `npm run typecheck`.</verify>
</task>

<task type="execute">
  <name>Stage and render the final Capitol-return sequence</name>
  <read_first>
    `03-UI-SPEC.md` Catch Sequence; `src/render/cutscenes/capture.ts`;
    `src/render/stage-renderer.ts`; approved Mitch/Capitol art modules; `public/styles.css`.
  </read_first>
  <files>
    `src/render/cutscenes/capture.ts`, `src/render/stage-renderer.ts`,
    `src/render/ui-renderer.ts`, `public/styles.css`
  </files>
  <action>
    Encode these beats: freeze/dim crowd, spotlight Mitch, FOUND HIM message, reaction, head
    retraction, spinning-shell transport along an original patriotic pneumatic trail, Capitol
    receipt, comic stamp/chime cue request, round-complete message. Animate transform/opacity of
    existing groups in the cutscene layer. Keep essential status readable when muted. Supply a
    reduced-motion staging using short fades/scales and at most one mild arc; show Skip only after
    one second and resolve it to the same final snapshot.
  </action>
  <acceptance_criteria>
    The visual sequence contains every required beat in order, remains nonviolent, pauses cleanly,
    works in reduced motion, and does not mutate score/round directly.
  </acceptance_criteria>
  <verify>Manual normal/reduced/muted review at desktop and 667×375; sequence unit tests.</verify>
</task>

<task type="execute">
  <name>Connect semantic capture completion to exactly one next round</name>
  <read_first>
    `src/game/controller.ts`, `src/core/reducer.ts`, `src/render/ui-renderer.ts`,
    `tests/e2e/capture-cutscene.spec.ts`, `docs/TEST-PLAN.md` outcome criteria.
  </read_first>
  <files>
    `src/game/controller.ts`, `src/render/ui-renderer.ts`,
    `tests/e2e/capture-cutscene.spec.ts`
  </files>
  <action>
    Start capture only after the reducer accepts `MITCH_CAUGHT`; synchronously leave playable input.
    On normal or skipped sequence completion, dispatch one `CAPTURE_COMPLETE`, create the next round
    through the standard round path, and reset attempts to ten. Add semantic beat/state markers for
    test waiting and a polite outcome announcement. E2E fixed-seed capture must observe all essential
    beats, completed-round increment, automatic transition, harder profile, and ten reset; also
    cover pause and skip without sleeps tied to raw duration.
  </action>
  <acceptance_criteria>
    Rapid target clicks/Skip cannot double score or skip a round; the next round starts without
    confirmation and uses the next difficulty profile.
  </acceptance_criteria>
  <verify>`npm run test:e2e -- capture-cutscene`; rerun reducer tests.</verify>
</task>

</tasks>

<verification>
Run all checks plus capture E2E in full/reduced motion and muted/unmuted states. Manually review
animation at supported viewports and inspect that no input is accepted during the outcome.
</verification>

<success_criteria>
Catching Mitch produces the full signature Capitol gag and always lands in exactly one correctly
initialized, harder next round.
</success_criteria>

<must_haves>
  <truths>
    OUT-01 and OUT-02 are visibly complete. Decisions D3-05, D3-07, and D3-08 hold under normal,
    paused, skipped, and reduced-motion paths.
  </truths>
  <artifacts>
    Generic sequence runner, capture definition, rendering integration, and E2E coverage exist.
  </artifacts>
  <key_links>
    Reducer authorizes capture; controller starts one outcome token; sequence renders approved rigs;
    completion dispatches one reducer event; reducer prepares the next round.
  </key_links>
</must_haves>

<threat_model>
No new external inputs exist. Token-check asynchronous completion to prevent stale mutation after
restart. Status messages use fixed local strings. Cutscene Skip resolves state rather than
fast-forwarding arbitrary untrusted callbacks.
</threat_model>
