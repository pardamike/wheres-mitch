---
phase: 04-scene-deck-and-variety
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/scenes/kentucky-fair.ts
  - src/render/art/fair.ts
  - src/world/behaviors.ts
  - src/world/scene-selector.ts
  - docs/ART-BIBLE.md
  - tests/unit/kentucky-fair.test.ts
  - tests/e2e/kentucky-fair.spec.ts
autonomous: false
requirements:
  - WORLD-02
---

<objective>
Author a richly animated Kentucky county fair that uses shared systems while remaining visually,
behaviorally, and spatially distinct from Washington.
</objective>

<execution_context>
Read the fair sections of `docs/GAME-DESIGN.md`, `docs/ART-BIBLE.md`, `04-CONTEXT.md`,
`04-UI-SPEC.md`, the final scene contract/common tests, current actor behaviors, and approved art
style. This plan ends with a human scene review.
</execution_context>

<tasks>

<task type="execute">
  <name>Author the fair composition, layers, and original prop art</name>
  <read_first>
    `04-UI-SPEC.md` Kentucky County Fair; `docs/ART-BIBLE.md` fair section;
    `src/scenes/kentucky-fair.ts`; `src/render/art/fair.ts`; Washington scene as a contract example.
  </read_first>
  <files>`src/scenes/kentucky-fair.ts`, `src/render/art/fair.ts`, `docs/ART-BIBLE.md`</files>
  <action>
    Build an original 1440×900 authored scene with late-day horizon, Ferris wheel, midway tents,
    pavilion/livestock edge, contest or stage area, picnic/queue zones, signs, fences, and hay-bale
    foreground. Separate static/ambient/actor/target/occluder/effects layers and provide cutscene
    landmarks. Use the approved warm palette and diverse crowd part library without rural or
    regional stereotypes. Add all new art to the asset manifest.
  </action>
  <acceptance_criteria>
    The setting reads as a lively county fair within five seconds, maintains HUD/target contrast,
    contains no real fair/brand logo, and all new art has a manifest entry.
  </acceptance_criteria>
  <verify>Render canonical desktop/mobile art boards; run manifest test; human visual review.</verify>
</task>

<task type="execute">
  <name>Encode valid fair routes, behaviors, hiding, occlusion, and variants</name>
  <read_first>
    `src/scenes/kentucky-fair.ts`, `src/world/behaviors.ts`, final scene/path contracts,
    `tests/unit/kentucky-fair.test.ts` if present.
  </read_first>
  <files>
    `src/scenes/kentucky-fair.ts`, `src/world/behaviors.ts`,
    `tests/unit/kentucky-fair.test.ts`
  </files>
  <action>
    Author lateral/looping routes, depth portals, vendor/food queue, picnic sitters, stage viewers,
    pavilion visitors, and prop interactions. Add only fair-specific behavior parameters/hooks that
    fit the existing routine model; do not branch generic actors on scene ID. Define reachable
    Mitch spots behind booths, signs, hay, fence, and passing foreground traffic with verified
    early reveals. Add curated palette/prop/crowd/route/hiding variants and common-contract plus
    fair-specific invariant tests.
  </action>
  <acceptance_criteria>
    The fair validates, demonstrates at least five purposeful activities, all Mitch spots are
    continuously reachable/click-correct, and seeds vary meaningful details reproducibly.
  </acceptance_criteria>
  <verify>`npm run test:run -- kentucky-fair scene-contract`; inspect all debug spots/routes.</verify>
</task>

<task type="execute">
  <name>Prove full fair gameplay and outcome integration</name>
  <read_first>
    `tests/e2e/kentucky-fair.spec.ts`, `docs/TEST-PLAN.md`, selector/controller/cutscene code.
  </read_first>
  <files>`src/world/scene-selector.ts`, `tests/e2e/kentucky-fair.spec.ts`</files>
  <action>
    Register the fair in the production scene registry and shuffle bag. Using canonical seed 324002,
    test start/ten attempts, exposed catch, real occlusion miss, capture sequence into another
    round, ten-miss escape/game over, pause, and mobile-landscape layout. Wait on semantic states;
    do not add fair-only test bypasses or coordinate fragile target clicks.
  </action>
  <acceptance_criteria>
    Every shared game path works in the fair, the selector can choose it normally, and existing
    Washington paths remain unchanged.
  </acceptance_criteria>
  <verify>`npm run test:e2e -- kentucky-fair`; full existing E2E suite.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve the Kentucky fair as a deep second setting</name>
  <what-built>
    Complete fair art, ambient activity, routes, target hides, variants, both outcomes, and
    desktop/mobile play.
  </what-built>
  <how-to-verify>
    Force `?scene=fair&seed=324002`, play a catch and loss, watch ambient activity for one minute,
    then try three other seeds at desktop and 667×375. Confirm fair identity, respectful crowd,
    target fairness, occlusion, and variety.
  </how-to-verify>
  <resume-signal>Owner approves the fair or supplies targeted visual/gameplay revisions.</resume-signal>
</task>

</tasks>

<verification>
Run all checks, fair/common tests, entire E2E suite, manifest audit, and human checkpoint. Confirm no
shared mega-component or scene-ID behavior branches were added.
</verification>

<success_criteria>
The Kentucky fair is a distinct deep setting with the same exact game rules, outcome quality,
determinism, and performance expectations as Washington.
</success_criteria>

<must_haves>
  <truths>
    WORLD-02 and decisions D4-01, D4-05, and D4-06 hold for the fair.
  </truths>
  <artifacts>
    Fair scene/art modules, asset entries, invariant tests, and browser flow tests exist.
  </artifacts>
  <key_links>
    Registry selects fair; generic world consumes its contract; shared cutscenes use its landmarks;
    common and scene-specific tests exercise the built scene.
  </key_links>
</must_haves>

<threat_model>
All content remains bundled/curated. Avoid trademarked logos and stereotyped depictions. Variants
select only safe authored values and cannot inject markup or create invalid route geometry.
</threat_model>
