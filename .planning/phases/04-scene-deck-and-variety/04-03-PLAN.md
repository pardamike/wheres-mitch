---
phase: 04-scene-deck-and-variety
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/scenes/airport.ts
  - src/render/art/airport.ts
  - src/world/behaviors.ts
  - src/world/scene-selector.ts
  - docs/ART-BIBLE.md
  - tests/unit/airport.test.ts
  - tests/e2e/airport.spec.ts
  - tests/e2e/scene-deck.spec.ts
  - tests/e2e/visual.spec.ts
autonomous: false
requirements:
  - WORLD-03
---

<objective>
Author the airport concourse, integrate and visually validate all three settings, and prove the
complete scene deck preserves deterministic rules and no-repeat selection.
</objective>

<execution_context>
Read airport sections of `docs/GAME-DESIGN.md`, `docs/ART-BIBLE.md`, `04-CONTEXT.md`,
`04-UI-SPEC.md`, final scene contract, existing Washington/fair scenes, and screenshot fixtures in
`docs/TEST-PLAN.md`.
</execution_context>

<tasks>

<task type="execute">
  <name>Author the airport composition and original prop art</name>
  <read_first>
    `04-UI-SPEC.md` Airport Concourse; `docs/ART-BIBLE.md` airport section;
    `src/scenes/airport.ts`; `src/render/art/airport.ts`; current scene examples.
  </read_first>
  <files>`src/scenes/airport.ts`, `src/render/art/airport.ts`, `docs/ART-BIBLE.md`</files>
  <action>
    Build an original 1440×900 concourse with windows/runway silhouettes, gate wall, departures
    board, seating banks, moving walkway, cafe/kiosk, columns, queue dividers, luggage flow, and
    foreground travelers. Layer static, ambient, actors, target, occluders, effects, and cutscene
    landmarks. Use the approved cool palette with warm cafe accents. Invent neutral symbols and
    destinations; do not reproduce airlines, airport brands, or alarming security imagery. Add
    all new art to the manifest.
  </action>
  <acceptance_criteria>
    The setting reads as an airport within five seconds, target/HUD remain legible, art is original,
    and no real travel branding or external asset exists.
  </acceptance_criteria>
  <verify>Render canonical desktop/mobile art boards; manifest test; human visual review.</verify>
</task>

<task type="execute">
  <name>Encode airport routes, activities, hiding, occlusion, and variants</name>
  <read_first>
    `src/scenes/airport.ts`, `src/world/behaviors.ts`, scene/path contracts, and
    `tests/unit/airport.test.ts` if present.
  </read_first>
  <files>`src/scenes/airport.ts`, `src/world/behaviors.ts`, `tests/unit/airport.test.ts`</files>
  <action>
    Author near/far transit lanes, moving-walkway progression, gate queue, seating, cafe visits,
    cleaning, luggage movement, and board/aircraft ambient systems using generic behavior hooks.
    Define continuous Mitch routes and reachable spots behind columns, kiosk, seats, luggage carts,
    and passing travelers with fair reveal windows. Add curated palette/board/prop/crowd/path/hide
    variants. Run common contracts and airport-specific tests for walkway/depth/spot invariants.
  </action>
  <acceptance_criteria>
    Airport validates, shows at least five purposeful activities, all hides are correctly occluded
    and reachable, and fixed seeds reproduce meaningful variants.
  </acceptance_criteria>
  <verify>`npm run test:run -- airport scene-contract`; inspect every debug route/spot.</verify>
</task>

<task type="execute">
  <name>Register, exercise, and visually baseline the complete scene deck</name>
  <read_first>
    `src/world/scene-selector.ts`; `tests/e2e/airport.spec.ts`, `scene-deck.spec.ts`,
    `visual.spec.ts`; `docs/TEST-PLAN.md` canonical fixtures; all three scene modules.
  </read_first>
  <files>
    `src/world/scene-selector.ts`, `tests/e2e/airport.spec.ts`,
    `tests/e2e/scene-deck.spec.ts`, `tests/e2e/visual.spec.ts`
  </files>
  <action>
    Register airport, then test canonical seed 324003 through catch, occluded miss, escape, pause,
    and mobile landscape. Test a deterministic 30-success sequence for all-scenes-per-bag and no
    immediate repeats. Add reviewed screenshots for canonical fixtures in `docs/TEST-PLAN.md` at
    desktop and 667×375, including one outcome frame per sequence. Mask nothing except browser-
    generated nondeterminism; reserve stable scene/debug state. Review diffs instead of blindly
    updating baselines.
  </action>
  <acceptance_criteria>
    All three scenes pass equivalent functional flows, deterministic selection, and approved visual
    baselines without clipping or target/layer regressions.
  </acceptance_criteria>
  <verify>`npm run test:e2e`; run/update visual baselines only after explicit human review.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve the complete three-scene deck</name>
  <what-built>
    Airport scene plus a fully rotating Washington/fair/airport deck with deterministic procedural
    variants and reviewed screenshots.
  </what-built>
  <how-to-verify>
    Play at least two seeds per scene and one normal unforced six-round run. Watch each setting for
    one minute, exercise a catch/loss, and review desktop/mobile screenshot diffs. Confirm instant
    scene identity, sufficient density, target fairness, visual originality, and no repetition.
  </how-to-verify>
  <resume-signal>Owner approves all three templates and the rotation or requests revisions.</resume-signal>
</task>

</tasks>

<verification>
Run all static/unit/E2E/visual/file tests and manifest audit, then the human checkpoint. Verify each
scene against the same contract and test no runtime fetch/dynamic import was introduced.
</verification>

<success_criteria>
The game contains three deep, distinct, alive worlds that rotate fairly and reproducibly while
sharing one simple runtime and full outcome loop.
</success_criteria>

<must_haves>
  <truths>
    WORLD-03 and decisions D4-01, D4-05 through D4-07 hold across the complete deck.
  </truths>
  <artifacts>
    Airport scene/art, asset entries, unit/E2E tests, scene-deck tests, and reviewed screenshots
    exist.
  </artifacts>
  <key_links>
    Selector registry knows all three definitions; generic world/controller renders each; common
    outcomes use scene landmarks; canonical tests cover equivalent behavior and presentation.
  </key_links>
</must_haves>

<threat_model>
All signage and brands are invented local art. Scene/debug values remain allowlisted. Screenshot
tests must not upload artifacts to a third party or include personal data; game still collects none.
</threat_model>
