---
phase: 04-scene-deck-and-variety
plan: "01"
type: execute
wave: 1
depends_on:
  - 03-03
files_modified:
  - src/world/scene.ts
  - src/world/scene-selector.ts
  - src/world/path-network.ts
  - src/scenes/washington.ts
  - src/game/controller.ts
  - tests/unit/scene-contract.test.ts
  - tests/unit/scene-selector.test.ts
  - tests/unit/scene-variation.test.ts
autonomous: true
requirements:
  - WORLD-05
  - WORLD-06
---

<objective>
Extract the proven minimum scene contract, add named deterministic variation streams, and select
all three templates through a reproducible no-repeat shuffle bag.
</objective>

<execution_context>
Read `04-CONTEXT.md`, `04-RESEARCH.md`, `04-UI-SPEC.md`, `docs/GAME-DESIGN.md` scene/procedural
sections, the current Washington/scene/world/controller code, and RNG tests. Preserve working
Washington behavior while generalizing only boundaries required by three scenes.
</execution_context>

<tasks>

<task type="execute">
  <name>Finalize and validate the minimum three-scene contract</name>
  <read_first>
    `src/world/scene.ts`, `src/world/path-network.ts`, `src/scenes/washington.ts`,
    `tests/unit/scene-contract.test.ts` if present, and `04-RESEARCH.md` Scene Contract.
  </read_first>
  <files>
    `src/world/scene.ts`, `src/world/path-network.ts`, `src/scenes/washington.ts`,
    `tests/unit/scene-contract.test.ts`
  </files>
  <action>
    Refine the existing contract only enough to express stable ID/title/view box, scene SVG
    construction, route graph, depth lanes, behavior anchors, hiding spots, variant tables, allowed
    palettes, actor-role weights, entry/exit landmarks, and debug landmarks. Add one reusable
    contract suite validating IDs, finite bounds, endpoints, route connectivity, hide reachability,
    required behavior anchors, nonempty variants, and cutscene entry/exit. Make Washington pass
    without changing its approved visual composition or creating a universal markup component.
  </action>
  <acceptance_criteria>
    A new authored scene can implement one typed interface; Washington passes the common contract;
    core controller/render paths contain no Washington-specific ordinary behavior.
  </acceptance_criteria>
  <verify>`npm run test:run -- scene-contract`; `npm run typecheck`; Phase 3 E2E.</verify>
</task>

<task type="execute">
  <name>Isolate procedural variation in stable named seed streams</name>
  <read_first>
    `src/core/rng.ts`, `src/world/scene.ts`, `src/scenes/washington.ts`,
    `tests/unit/scene-variation.test.ts`, and `docs/TEST-PLAN.md` Determinism Contract.
  </read_first>
  <files>
    `src/world/scene.ts`, `src/scenes/washington.ts`, `tests/unit/scene-variation.test.ts`
  </files>
  <action>
    Derive `scene`, `crowd`, `mitch`, and `cosmetic` streams from each round seed and pass only the
    relevant stream into each subsystem. Select Washington palette accents, crowd identities,
    authored prop variants, route choices, and hiding choices from validated tables. Return selected
    variant IDs in explicit debug state. Test same-seed identity, meaningful different-seed changes,
    and that adding/consuming a cosmetic choice does not alter Mitch route decisions.
  </action>
  <acceptance_criteria>
    Seeds change meaningful authored details without invalid geometry; unrelated random-choice code
    cannot cascade into target routes; fixed seeds remain screenshot-stable.
  </acceptance_criteria>
  <verify>`npm run test:run -- scene-variation rng`; compare two debug snapshots.</verify>
</task>

<task type="execute">
  <name>Implement a run-scoped deterministic shuffle bag</name>
  <read_first>
    `src/world/scene-selector.ts`, `src/game/controller.ts`, `src/core/rng.ts`, and
    `tests/unit/scene-selector.test.ts` if present.
  </read_first>
  <files>
    `src/world/scene-selector.ts`, `src/game/controller.ts`,
    `tests/unit/scene-selector.test.ts`
  </files>
  <action>
    Implement seeded Fisher–Yates selection across the registered scene IDs. Emit each ID once per
    bag; when refilling, swap the first entry if it equals the prior scene. Reset the bag on Play
    Again. Keep the allowlisted `?scene=` debug override separate so it does not corrupt subsequent
    normal bag order. Unit-test full-bag coverage, boundary no-repeat, deterministic sequences,
    restart reset, and invalid override fallback.
  </action>
  <acceptance_criteria>
    Any normal sequence shows all three scenes before reuse, never repeats immediately, and exactly
    reproduces for the same run seed.
  </acceptance_criteria>
  <verify>`npm run test:run -- scene-selector`; print and inspect a 30-round fixed-seed sequence.</verify>
</task>

</tasks>

<verification>
Run all tests through Phase 3 plus new scene tests. Compare Washington fixed-seed screenshots before
and after; any visual change requires explicit intent and review. Search controller/runtime for
scene-ID branches and justify or remove ordinary ones.
</verification>

<success_criteria>
The runtime has a small proven scene seam, procedural variety does not destabilize target behavior,
and the deterministic selector is ready for two additional templates.
</success_criteria>

<must_haves>
  <truths>
    WORLD-05 and WORLD-06 hold. Decisions D4-02 through D4-04 and D4-07 are enforced by contract and
    unit tests.
  </truths>
  <artifacts>
    Final scene contract, variant logic, selector, converted Washington definition, and tests exist.
  </artifacts>
  <key_links>
    Controller asks selector for a scene ID; registry supplies one scene definition; named streams
    instantiate variation/world/target independently; common validation covers every definition.
  </key_links>
</must_haves>

<threat_model>
Debug scene input is allowlisted and never treated as a module/path. Bounded authored tables prevent
arbitrary geometry or text injection. Shuffle and seed arithmetic stay uint32 and finite.
</threat_model>

